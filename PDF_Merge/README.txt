# PDF_Merge

This script opens an explorer window and allows user to drag and drop in as many pdf files as you want.

Then the compiler merges all files together into a single pdf file and stores it in a merged folder for accessing.
